# doc-driven-dev
A project to test the following concept: the base of the repo is the product description (Strategy, vision, user flows, desired behaviour). After a change in the product description a pull request and connected issue will be created. 
